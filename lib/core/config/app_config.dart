/// Central app identity constants.
///
/// Update [kAppName] here to rename the app across all user-visible surfaces.
/// The Dart package name (pubspec.yaml `name:`) is intentionally kept
/// separate — changing it requires updating every `package:` import.
const String kAppName = 'SST Cam';

/// SQLite database filename stored in the app-private support directory.
const String kDbName = 'sst_cam.sqlite';
