services:
  workspace:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspace:cached
      - ~/.claude:/root/.claude
      - ~/.local/bin/claude:/root/.local/bin/claude:ro
    command: sleep infinity
